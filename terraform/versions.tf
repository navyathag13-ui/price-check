terraform {
  required_version = ">= 1.5.0"
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 4.0"
    }
  }
}

provider "azurerm" {
  features {}
  # Auth via `az login` (Cloud Shell) or a service principal in CI (ARM_* env vars) -- never hardcoded.
}
